#include <iostream>
#include <cmath>
using namespace std;

int main() {

	setlocale(0, "");

	int a, b, c, x;

	cout << "Введите значение переменных A, B, C : " << endl;

	cin >> a >> b >> c;

	(a > b & b > c) ? (x = a + b) : ((a > b & c > b) ? (x = a + c) : (x = b + c));

	cout << "Сумма больших значений равна : " << x << endl;

	cout << endl;

	system("pause");

	return 0;

}
