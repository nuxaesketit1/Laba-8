#include <iostream>
#include <cmath>
using namespace std;

int main() {

	setlocale(0, "");

	int a, b;

	cout << "Введите значение переменных A и B : " << endl;

	cin >> a >> b;

	(a == b) ? (a = 0, b = 0) : (a > b ? (b = a) : (a = b));

	cout << "Результат : " << a << " " << b << endl;

	cout << endl;

	system("pause");

	return 0;

}
