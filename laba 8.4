#include <iostream>
#include <cmath>
using namespace std;

int main() {

	setlocale(0, "");

	double x, y;

	cout << "Введите координаты точки ( x , y ) : " << endl;

	cin >> x >> y;

	if ((x > 0) & (y > 0)) {

		cout << "Точка в первой координатной четверти!" << endl;

	}
	else
	{
		if ((x < 0) & (y > 0)) {

			cout << "Точка в второй координатной четверти!" << endl;

		}
		else
		{
			if ((x < 0) & (y < 0)) {
				cout << "Точка в третьей координатной четверти!" << endl;
			}
			else
			{
				cout << "Точка в четвертой координатной четверти" << endl;
			}
		}
	}

	cout << endl;

	system("pause");

	return 0;

}
